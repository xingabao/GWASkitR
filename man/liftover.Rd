% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/liftover.R
\name{liftover}
\alias{liftover}
\title{Perform Genomic Coordinate Liftover Between Builds}
\usage{
liftover(
  sumstats,
  build.from = "hg38",
  build.to = "hg19",
  chain.file = NULL,
  chain.source = "ensembl",
  imputation.ind = FALSE,
  chrom.col = "CHR",
  start.col = "POS",
  as.granges = FALSE,
  GRanges.style = "NCBI",
  verbose = TRUE,
  save.path = NULL
)
}
\arguments{
\item{sumstats}{Data frame, input summary statistics data with chromosomal positions.}

\item{build.from}{Character, source genome build (e.g., 'hg38'; default: 'hg38').}

\item{build.to}{Character, target genome build (e.g., 'hg19'; default: 'hg19').}

\item{chain.file}{Character, path to a custom chain file for liftover (default: NULL, auto-download).}

\item{chain.source}{Character, source of chain file if not provided ('ensembl' or 'ucsc'; default: 'ensembl').}

\item{imputation.ind}{Logical, whether to include an indicator column for imputation (default: FALSE).}

\item{chrom.col}{Character, column name for chromosome in input data (default: 'CHR').}

\item{start.col}{Character, column name for start position in input data (default: 'POS').}

\item{as.granges}{Logical, whether to return results as a GRanges object (default: FALSE).}

\item{GRanges.style}{Character, style for GRanges object ('NCBI' or 'UCSC'; default: 'NCBI').}

\item{verbose}{Logical, whether to print progress messages (default: TRUE).}

\item{save.path}{Character, path to save the lifted-over summary statistics (default: NULL, no saving).}
}
\value{
A data frame or GRanges object (based on \code{as.granges}) containing the lifted-over
  summary statistics with updated genomic coordinates.
}
\description{
This function converts genomic coordinates from one genome build to another (e.g., hg38 to hg19)
using a chain file for liftover. It supports conversion of summary statistics data and can return
results as a data frame or GRanges object.
}
\examples{
\dontrun{
  sumstats <- data.frame(CHR = c(1, 2), POS = c(1000, 2000), SNP = c("rs1", "rs2"))
  result <- liftover(sumstats, build.from = "hg38", build.to = "hg19", save.path = "output/path.csv")
}

}
