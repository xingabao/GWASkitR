% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.file.path.R
\name{check.file.path}
\alias{check.file.path}
\title{Check if a file path is valid and has the correct suffix}
\usage{
check.file.path(
  file.path,
  suffix = c("txt", "tsv"),
  verbose = FALSE,
  stop_on_error = FALSE
)
}
\arguments{
\item{file.path}{A character string specifying the file path to check.}

\item{suffix}{A character vector of allowed suffixes (e.g., c('txt', 'tsv')).}

\item{verbose}{Logical. If TRUE, log info-level messages.}

\item{stop_on_error}{Logical. If TRUE, stop with error when check fails. Default is FALSE (just return FALSE).}
}
\value{
TRUE if file.path is valid and suffix matches; FALSE otherwise (unless stop_on_error is TRUE, then error thrown).
}
\description{
This function checks whether the given file path is non-null, non-empty, and ends with one of the allowed suffixes.
It logs warnings and errors using the logger package. Optionally, it logs info-level messages when verbose is TRUE.
}
\examples{
check.file.path("test.txt")           # TRUE
check.file.path("data.csv", suffix = c("csv", "tsv"))  # TRUE
check.file.path(NULL)                 # FALSE
check.file.path("")                   # FALSE
check.file.path("file.doc", suffix = c("txt", "tsv"))  # FALSE

}
